{% extends "layout.html.twig" %}

{% block content %}
    <ul>
        {% for post in posts %}
            <li> {{ post.title }} {{ post.content }}</li>
        {% else %}
            <li><em>no posts found</em></li>
        {% endfor %}
    </ul>
{% endblock %}
